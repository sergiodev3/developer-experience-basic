// Contenido genérico de ejemplo para la Clase 1.
// Versión final después de los 3 pasos del Bloque 2: primero se probó en el
// REPL de Node, luego se guardó como archivo, y por último se convirtió en
// una función reutilizable.

function saludar(nombre) {
  return `Hola, ${nombre}!`;
}

console.log(saludar("mundo"));
